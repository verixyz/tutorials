import { IKey, ManagedKeyInfo } from '@verixyz/core';
export declare abstract class AbstractKeyStore {
    abstract import(args: Partial<IKey>): Promise<boolean>;
    abstract get(args: {
        kid: string;
    }): Promise<IKey>;
    abstract delete(args: {
        kid: string;
    }): Promise<boolean>;
    abstract list(args: {}): Promise<Array<ManagedKeyInfo>>;
}
//# sourceMappingURL=abstract-key-store.d.ts.map