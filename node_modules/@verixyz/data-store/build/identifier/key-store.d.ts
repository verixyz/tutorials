import { IKey, ManagedKeyInfo } from '@verixyz/core';
import { AbstractKeyStore } from '@verixyz/key-manager';
import { Connection } from 'typeorm';
export declare class KeyStore extends AbstractKeyStore {
    private dbConnection;
    constructor(dbConnection: Promise<Connection>);
    get({ kid }: {
        kid: string;
    }): Promise<IKey>;
    delete({ kid }: {
        kid: string;
    }): Promise<boolean>;
    import(args: IKey): Promise<boolean>;
    list(args?: {}): Promise<ManagedKeyInfo[]>;
}
//# sourceMappingURL=key-store.d.ts.map