import { AbstractSecretBox, AbstractPrivateKeyStore } from '@verixyz/key-manager';
import { Connection } from 'typeorm';
import { ImportablePrivateKey, ManagedPrivateKey } from '@verixyz/key-manager/src/abstract-private-key-store';
export declare class PrivateKeyStore extends AbstractPrivateKeyStore {
    private dbConnection;
    private secretBox?;
    constructor(dbConnection: Promise<Connection>, secretBox?: AbstractSecretBox | undefined);
    get({ alias }: {
        alias: string;
    }): Promise<ManagedPrivateKey>;
    delete({ alias }: {
        alias: string;
    }): Promise<boolean>;
    import(args: ImportablePrivateKey): Promise<ManagedPrivateKey>;
    list(): Promise<Array<ManagedPrivateKey>>;
}
//# sourceMappingURL=private-key-store.d.ts.map