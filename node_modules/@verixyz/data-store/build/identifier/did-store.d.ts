import { IIdentifier } from '@verixyz/core';
import { AbstractDIDStore } from '@verixyz/did-manager';
import { Connection } from 'typeorm';
export declare class DIDStore extends AbstractDIDStore {
    private dbConnection;
    constructor(dbConnection: Promise<Connection>);
    get({ did, alias, provider, }: {
        did: string;
        alias: string;
        provider: string;
    }): Promise<IIdentifier>;
    delete({ did }: {
        did: string;
    }): Promise<boolean>;
    import(args: IIdentifier): Promise<boolean>;
    list(args: {
        alias?: string;
        provider?: string;
    }): Promise<IIdentifier[]>;
}
//# sourceMappingURL=did-store.d.ts.map